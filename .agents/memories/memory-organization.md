# Memory organization

**NEVER append unrelated project knowledge to an existing memory file.** Each
memory file must cover one cohesive topic that its filename and top-level heading
describe. Before updating a memory, verify that the new fact belongs to that exact
topic; otherwise create or update a dedicated memory file. Periodically extended
topics may share a file only when the new material directly refines or supersedes
the same subject.
